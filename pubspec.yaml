name: sign_in_with_kakao
description: A new Flutter plugin.
version: 1.0.1
homepage: https://github.com/dev-juyoung/sign_in_with_kakao

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: dev.juyoung.sign_in_with_kakao
        pluginClass: SignInWithKakaoPlugin
      ios:
        pluginClass: SignInWithKakaoPlugin
